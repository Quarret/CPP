#include <iostream>
using namespace std;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    long long a, b, ans;
    cin >> a >> b;
    ans = a / b;

    cout << ans << '\n';


    return 0;
}
