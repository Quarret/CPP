#include <bits/stdc++.h>
using i64 = long long;
using namespace std;

long long countDistinct(long long n) {
    string high_s = to_string(n);
    int m = high_s.size();
    
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}
