#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int maxMoves(vector<vector<int>>& grid) {
    int m = grid.size(), n = grid[0].size();
    vector f(m + 2, vector<int>(n + 2, INT32_MAX));
    
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}
