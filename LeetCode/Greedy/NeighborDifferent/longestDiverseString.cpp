#include <bits/stdc++.h>
using i64 = long long;
using namespace std;

string longestDiverseString(int a, int b, int c) {
    int n = a + b + c;
    if (n == a || n == b || n == c) {
        return "";
    }
    
    int m = max({a, b, c});
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}
