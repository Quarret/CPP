#include <bits/stdc++.h>
using ll = long long;
using namespace std;

bool isPowerOfFour(int n) {
    return n > 0 && __builtin_popcount(n) == 1 && __bit_width(n) % 2;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}
