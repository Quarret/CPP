#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int longestValidParentheses(string s) {
    int n = s.size();
    int maxx = 0, cnt = 0;
    vector<char> st;

    for (int i = 0; i < n; i++) {
        
    }
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}
