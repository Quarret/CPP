#include <bits/stdc++.h>
using ll = long long;
using namespace std;

class Solution {
public:
    vector<int> processQueries(vector<int>& queries, int m) {
        
    }
};
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}
